import React,{useState}from'react';import{Navigate}from'react-router-dom';import{Zap}from'lucide-react';import{base44}from'@/api/base44Client';import{isSupabaseConfigured}from'@/api/supabaseClient';import{useAuth}from'@/lib/AuthContext';
export default function Login(){const{isAuthenticated,checkAppState}=useAuth();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(isAuthenticated)return <Navigate to="/" replace/>;async function submit(e){e.preventDefault();setError('');setBusy(true);try{await base44.auth.loginViaEmailPassword(email,password);await checkAppState()}catch(err){setError(err.message)}finally{setBusy(false)}}return <div className="grid min-h-screen place-items-center bg-background p-6"><form onSubmit={submit} className="w-full max-w-md rounded-2xl border border-border bg-card p-8 shadow-xl"><div className="mb-8 flex items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-xl bg-amber-400 text-slate-950"><Zap/></div><div><h1 className="text-2xl font-black">Estim8r</h1><p className="text-sm text-muted-foreground">Professional Electrical Estimating</p></div></div>{!isSupabaseConfigured&&<div className="mb-5 rounded-lg border border-amber-500/40 bg-amber-500/10 p-3 text-sm">Supabase is not configured.</div>}{error&&<div className="mb-5 rounded-lg border border-red-500/40 bg-red-500/10 p-3 text-sm text-red-600">{error}</div>}<label className="mb-2 block text-sm">Email</label><input value={email} onChange={e=>setEmail(e.target.value)} type="email" required className="mb-5 w-full rounded-lg border border-input bg-background px-3 py-3"/><label className="mb-2 block text-sm">Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" required className="mb-6 w-full rounded-lg border border-input bg-background px-3 py-3"/><button disabled={busy||!isSupabaseConfigured} className="w-full rounded-lg bg-amber-400 px-4 py-3 font-bold text-slate-950 disabled:opacity-50">{busy?'Signing in…':'Sign in'}</button></form></div>}
